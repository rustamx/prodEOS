// Добавляет запись.
//
Процедура ДобавитьЗапись(Письмо, ИдентификаторИсточника, ИдентификаторОснования) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	Запись = СоздатьМенеджерЗаписи();
	Запись.Письмо = Письмо;
	Запись.ИдентификаторИсточника = ИдентификаторИсточника;
	запись.ИдентификаторОснования = ИдентификаторОснования;	
	Запись.Записать(Истина);
	
КонецПроцедуры

// Возвращает идентификатор основания по письму.
//
Функция ПолучитьИдентификаторОснования(Письмо) Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	Запись = СоздатьМенеджерЗаписи();
	Запись.Письмо = Письмо;
	Запись.Прочитать();
	Если Не Запись.Выбран() Тогда
		Возврат Неопределено;
	КонецЕсли;
	
	Возврат Запись.ИдентификаторОснования;
	
КонецФункции

